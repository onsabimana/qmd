#!/usr/bin/env bun
/**
 * MCP Server module
 * This is the main entry point for the MCP server
 */

// The MCP server is executable, so we just re-export everything from mcp.ts
// which contains the server implementation
import "./mcp";
